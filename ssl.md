# SSL Configuration

SSL/TLS will be configured to secure the website connection using HTTPS.

The SSL certificate will be installed after connecting the domain name to the server.

HTTPS will help encrypt communication between the user and the web server.

